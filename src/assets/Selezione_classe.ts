


const validClass(elenco: string[], input_utente: string): boolean {
    let flag = false;
    for (let s of elenco) {
        if (input_utente === s) {
            flag = true;
            break;
        }
    }
    return flag;
}

const 

function validCaratteristPoit(points:number[]): number{
    let number =0;
    for(let s of points){

    }
    return 0;
}

class ClassUtil {
    test=():number =>{
        return 1;
    }
    trota=():number =>{
        return 1;
    }


}
